{
  "$schema": "../gen/schemas/capability-schema.json",
  "identifier": "default",
  "description": "Minimal permissions for MANPADS control suite",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
